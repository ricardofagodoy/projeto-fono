<!DOCTYPE html>
<html>
    <head>
        <title><%= settings.title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>        
        <h1><%= settings.title %></h1>
        
        <h3 style="color: green"><%= typeof(success)!== 'undefined' ? 'Photo ' + success + ' uploaded with success!': '' %></h3>
        
        <p>Faça upload da imagem para o catálogo!</p>
            <form method='post' enctype='multipart/form-data'>
            <p>
                <input type='text' name='photo[name]' placeholder='Nome da imagem'/>
            </p>

            <p>
                <input type='file' name='photo[image]'/>
            </p>

            <p>
                <input type='submit' value='Upload!'/>
            </p>
        </form>
    </body>
</html>